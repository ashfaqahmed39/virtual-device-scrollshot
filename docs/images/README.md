# Documentation Images

The root README uses `demo.png` for Android and `screenshotios.png` for iOS demonstration captures.

Only add images that do not contain secrets, personal information, access tokens, or private application data.
